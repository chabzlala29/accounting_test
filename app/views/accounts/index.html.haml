.inner.cover
  %h1.cover-heading Accounting Amazing
  %p.lead This app keeps track of all accounting transactions from RailsExample
- if @accounts.any?
  %table.table.table-bordered
    %thead
      %tr
        %th Account ID
        %th Name
        %th Actions
    %tbody
      - @accounts.each do |account|
        %tr
          %th{:scope => "row"}= account.id
          %td= account.name
          %td
            = link_to 'Delete Account', account, method: :delete
            = '|'
            = link_to 'Update Account', edit_account_path(account)
            = '|'
            = link_to 'View Postings', account_postings_path(account)

- else
  %h3 No Postings available

= render 'buttons'
